services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: taskboard
      POSTGRES_PASSWORD: taskboard
      POSTGRES_DB: taskboard
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - redisdata:/data

  web:
    build: .
    command: npm run dev
    volumes:
      - .:/app
      - node_modules:/app/node_modules
    ports:
      - "3000:3000"
    depends_on:
      - db
      - redis
    environment:
      DATABASE_URL: postgresql://taskboard:taskboard@db:5432/taskboard?schema=public
      JWT_SECRET: dev-secret-change-me
      NODE_ENV: development
      REDIS_URL: redis://redis:6379
      AIRTABLE_API_KEY: ${AIRTABLE_API_KEY:-}
      AIRTABLE_BASE_ID: ${AIRTABLE_BASE_ID:-}
      AIRTABLE_TABLE_NAME: ${AIRTABLE_TABLE_NAME:-Tasks}

  worker:
    build: .
    command: npm run worker
    volumes:
      - .:/app
      - node_modules:/app/node_modules
    depends_on:
      - db
      - redis
    environment:
      DATABASE_URL: postgresql://taskboard:taskboard@db:5432/taskboard?schema=public
      NODE_ENV: development
      REDIS_URL: redis://redis:6379
      AIRTABLE_API_KEY: ${AIRTABLE_API_KEY:-}
      AIRTABLE_BASE_ID: ${AIRTABLE_BASE_ID:-}
      AIRTABLE_TABLE_NAME: ${AIRTABLE_TABLE_NAME:-Tasks}

  test:
    build: .
    command: npm test
    volumes:
      - .:/app
      - node_modules:/app/node_modules
    depends_on:
      - db
    environment:
      DATABASE_URL: postgresql://taskboard:taskboard@db:5432/taskboard_test?schema=public
      JWT_SECRET: test-secret
      NODE_ENV: test

volumes:
  pgdata:
  redisdata:
  node_modules:
